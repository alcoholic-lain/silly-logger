[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "silly-logger"
version = "0.1.0"
description = "just a silly logger :P"
requires-python = ">=3.8"
dependencies = ["requests"]

[dependency-groups]
dev = [
    "hatch>=1.14.2",
    "twine>=6.1.0",
]


